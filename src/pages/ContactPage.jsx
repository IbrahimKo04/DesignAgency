import { Contact } from "@/components/Contact";

export const ContactPage = () => {
  return (
    <>
      {/* Reusing the existing Contact component as a full page */}
      <Contact />
    </>
  );
};